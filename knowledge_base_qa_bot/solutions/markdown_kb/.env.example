# The OPENAI_API_KEY is shared — set it once in solutions/.env (see ../.env.example).
# Create a local .env HERE only to override settings for this solution; it takes
# precedence over the shared file. Defaults shown:
# CHAT_MODEL=gpt-4o-mini
# DOCS_DIR=docs
# KB_DIR=.kb
# TOP_K=4
# SCORE_THRESHOLD=0.0
# OPENAI_TIMEOUT=30
