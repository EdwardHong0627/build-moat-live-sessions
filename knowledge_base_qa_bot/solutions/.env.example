# Shared secret for BOTH solutions. Copy to solutions/.env and fill in your key.
# solutions/.env is git-ignored and loaded automatically by both apps on startup.
OPENAI_API_KEY=sk-...

# Optional shared overrides (apply to both unless a per-solution .env overrides them):
# CHAT_MODEL=gpt-4o-mini
# OPENAI_TIMEOUT=30
